<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="MaterialTheme.Light.TranslucentStatus">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryLight</item>
        <item name="colorAccent">@color/colorPrimaryLight</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="animation">
        <item name="android:windowEnterAnimation">@anim/popupshow</item>
        <item name="android:windowExitAnimation">@anim/popuphide</item>
    </style>
    <style name="AppTheme.CustomThemePOP" >

            <item name="android:colorBackgroundCacheHint">@null</item>
            <item name="android:windowIsTranslucent">true</item>
            <item name="android:windowAnimationStyle">@android:style/Animation</item>
            <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:windowBackground">@drawable/roundedcorners</item>
        </style>

    <style name="Rounder" parent="AppTheme.CustomThemePOP">
        <item name="android:windowBackground">@drawable/roundedcorners</item>
    </style>
</resources>
